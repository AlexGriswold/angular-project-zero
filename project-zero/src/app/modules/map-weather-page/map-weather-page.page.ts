import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-map-weather-page',
  templateUrl: './map-weather-page.page.html',
  styleUrls: ['./map-weather-page.page.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class MapWeatherPageComponent {

  constructor(

  ) { }
}
